{
  "rules": {
    ".read": "auth != null",
    ".write": "auth != null",
    
    "users": {
       ".indexOn": ["email"]
    },
    "requests": {
       ".indexOn": ["createdAt", "updatedAt", "companyId", "unitId", "creatorId"]
    }
  }
}